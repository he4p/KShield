id = "mount-activity"
name = "Mount Activity"
description = "Flags mount syscall activity for manual review."
severity = "warning"
tags = ["filesystem", "persistence"]
summary_template = "{comm} attempted mount on {subject}"

[match]
event_types = ["mount"]
